version: "2.3"

services:
  dynamodb-local:
    image: amazon/dynamodb-local
    container_name: dynamodb
    ports:
      - "8000:8000"

  redis:
    image: "redis:alpine"
    container_name: redis
    ports:
      - "6379:6379"
